apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.pdf.name }}
  labels:
    app: {{ .Values.pdf.name }}
spec:
  type: {{ .Values.pdf.service.type }}
  ports:
  - port: {{ .Values.pdf.service.port }}
    targetPort: {{ .Values.pdf.containerPort }}
    protocol: TCP
    name: http
  selector:
    app: {{ .Values.pdf.name }}

