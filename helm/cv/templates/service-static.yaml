apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.static.name }}
  labels:
    app: {{ .Values.static.name }}
spec:
  type: {{ .Values.static.service.type }}
  ports:
  - port: {{ .Values.static.service.port }}
    targetPort: {{ .Values.static.containerPort }}
    protocol: TCP
    name: http
  selector:
    app: {{ .Values.static.name }}

